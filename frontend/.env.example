# Copy this file to frontend/.env only if your backend does not run on the default URL.
# The frontend falls back to http://127.0.0.1:8000 when this file is absent.
# Never put API secrets in the frontend.
VITE_API_BASE_URL=http://127.0.0.1:8000
